module engine.render.gpu;

public {
    import
        engine.render.gpu.texture,
        engine.render.gpu.types,
        engine.render.gpu.buffers,
        engine.render.gpu.shader,
        engine.render.gpu.framebuffer;
}

