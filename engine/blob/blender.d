//*****************************************************************************
//
// Blender file loader (placeholder): I'm most interested to load
// static objects from blender files, but of course, it is useful for
// other things, too (bitmaps, animations, ...).
//
// It could be a nice challenge to the engine to reimplement "Yo Frankie!"
// (or some other blender game) using original Blender files.
//
//*****************************************************************************

module engine.blob.blender;

