

                            STATIC LIBRARIES
                            ----------------

							

This directory contains D modules, which are compiled and linked to engine and
games.

For other libraries, I try to use system side dynamic ones, but there may be
exceptions to this rule to allow static linking. This directory is also for them.

