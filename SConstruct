###############################################################################
#
# SCons file to configure build environment
#
###############################################################################

import os

print("")
print("Preliminary!")
print("At some point, running scons on root directory might run installations etc.")
print("")

env = Environment()

env.SConscript("engine/build/scons/config.py", "env")
